version: 0.0
resources:
  - myLambdaFunction:
      Type: AWS::Lambda::Function
      Properties:
        Name: "LambdaDynamoDBApp"  # Your Lambda function name
        Alias: "prod"              # The alias you created
        CurrentVersion: 1          # The current published version number
        TargetVersion: 2           # The new published version number
 